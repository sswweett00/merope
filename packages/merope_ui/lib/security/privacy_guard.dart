import 'package:flutter/material.dart';

class PrivacyGuard extends StatelessWidget {
  final Widget child;

  const PrivacyGuard({super.key, required this.child});

  @override
  Widget build(BuildContext context) => child;
}
